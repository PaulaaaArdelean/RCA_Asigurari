@page
@model RCA_Asigurari.Pages.Localitati.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Localitate[0].Localitatea)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Localitate[0].Judet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Localitate) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Localitatea)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Judet.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
